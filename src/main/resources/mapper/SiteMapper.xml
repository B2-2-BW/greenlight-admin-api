<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.winten.greenlight.admin.db.repository.mapper.site.SiteMapper">

    <select id="findSiteById" parameterType="SiteInfo" resultType="SiteInfo">
        SELECT
            site_id,
            site_name,
            site_description,
            site_api_key,
            site_enabled,
            queue_enabled,
            created_by,
            created_at,
            created_ip,
            updated_by,
            updated_at,
            updated_ip,
            deleted_by,
            deleted_at,
            deleted_ip
        FROM site_info
        WHERE site_id = #{siteId}
          AND deleted_at IS NULL
    </select>

    <select id="findAllSite">
        SELECT
            site_id,
            site_name,
            site_description,
            site_api_key,
            site_enabled,
            queue_enabled,
            created_by,
            created_at,
            created_ip,
            updated_by,
            updated_at,
            updated_ip,
            deleted_by,
            deleted_at,
            deleted_ip
        FROM site_info
        WHERE deleted_at IS NULL
    </select>

    <sql id="siteSearchWhere">
        <where>
            deleted_at IS NULL
            <if test="siteIds != null and siteIds.size() > 0">
                AND site_id IN
                <foreach collection="siteIds" item="id" open="(" separator="," close=")">
                    #{id}
                </foreach>
            </if>
            <if test="enabled != null">AND site_enabled = #{enabled}</if>
            <if test="query != null and query != ''">
                AND (LOWER(site_id) LIKE CONCAT('%', LOWER(#{query}), '%')
                OR LOWER(site_name) LIKE CONCAT('%', LOWER(#{query}), '%')
                OR LOWER(COALESCE(site_description, '')) LIKE CONCAT('%', LOWER(#{query}), '%'))
            </if>
        </where>
    </sql>

    <select id="findSitesPage" resultType="SiteInfo">
        SELECT site_id, site_name, site_description, site_enabled, queue_enabled, created_by, created_at, created_ip, updated_by, updated_at, updated_ip
        FROM site_info
        <include refid="siteSearchWhere"/>
        ORDER BY site_id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countSites" resultType="long">
        SELECT COUNT(*) FROM site_info
        <include refid="siteSearchWhere"/>
    </select>

    <select id="existsBySiteApiKey" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM site_info WHERE site_api_key = #{siteApiKey})
    </select>

    <select id="existsBySiteIdIncludingDeleted" resultType="boolean">
        SELECT EXISTS(SELECT 1 FROM site_info WHERE site_id = #{siteId})
    </select>

    <update id="updateSiteInfoById" parameterType="SiteInfo">
        UPDATE site_info
        SET
            <if test="siteName != null and siteName != ''"> site_name = #{siteName}, </if>
            <if test="siteDescription != null"> site_description = #{siteDescription}, </if>
            <if test="siteEnabled != null"> site_enabled = #{siteEnabled}, </if>
            <if test="queueEnabled != null"> queue_enabled = #{queueEnabled}, </if>
            updated_by = #{updatedBy},
            updated_ip = #{updatedIp},
            updated_at = CURRENT_TIMESTAMP
        WHERE site_id = #{siteId}
          AND deleted_at IS NULL
    </update>

    <update id="updateSiteApiKey" parameterType="SiteInfo">
        UPDATE site_info
        SET site_api_key = #{siteApiKey},
            updated_by = #{updatedBy},
            updated_ip = #{updatedIp},
            updated_at = CURRENT_TIMESTAMP
        WHERE site_id = #{siteId}
          AND deleted_at IS NULL
    </update>

    <insert id="insertSite" parameterType="SiteInfo">
        INSERT INTO site_info (
            site_id,
            site_name,
            site_description,
            site_api_key,
            site_enabled,
            queue_enabled,
            created_by,
            created_ip,
            updated_by,
            updated_ip
        ) VALUES (
            #{siteId},
            #{siteName},
            #{siteDescription},
            #{siteApiKey},
            #{siteEnabled},
            #{queueEnabled},
            #{createdBy},
            #{createdIp},
            #{updatedBy},
            #{updatedIp}
        )
    </insert>

    <update id="softDeleteSite" parameterType="SiteInfo">
        UPDATE site_info
        SET site_enabled = false,
            queue_enabled = false,
            deleted_by = #{updatedBy},
            deleted_at = CURRENT_TIMESTAMP,
            deleted_ip = #{updatedIp},
            updated_by = #{updatedBy},
            updated_at = CURRENT_TIMESTAMP,
            updated_ip = #{updatedIp}
        WHERE site_id = #{siteId}
          AND deleted_at IS NULL
    </update>
</mapper>
